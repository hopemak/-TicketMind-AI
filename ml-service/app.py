cd /a/task2/ai-support-ticket-classification/ml-service && python -c "open('utils.py','w').write('import re\nimport string\nimport math\nimport json\nimport pickle\nimport os\nimport sys\n\nNLTK_AVAILABLE = False\nlemmatizer = None\nstop_words = set()\n\ndef _init_nltk():\n    global NLTK_AVAILABLE, lemmatizer, stop_words\n    try:\n        import nltk\n        from nltk.corpus import stopwords as nltk_stopwords\n        from nltk.stem import WordNetLemmatizer as WNL\n        try:\n            nltk.data.find(\"corpora/stopwords\")\n        except LookupError:\n            try:\n                nltk.download(\"stopwords\", quiet=True)\n            except:\n                pass\n        try:\n            nltk.data.find(\"corpora/wordnet\")\n        except LookupError:\n            try:\n                nltk.download(\"wordnet\", quiet=True)\n            except:\n                pass\n        lemmatizer = WNL()\n        stop_words = set(nltk_stopwords.words(\"english\"))\n        NLTK_AVAILABLE = True\n    except:\n        NLTK_AVAILABLE = False\n        lemmatizer = None\n        stop_words = set()\n\n_init_nltk()\n\ndef preprocess_text(text):\n    if not text or not isinstance(text, str):\n        return \"\"\n    text = text.lower()\n    text = re.sub(r\"http\\S+|www\\S+|https\\S+\", \"\", text, flags=re.MULTILINE)\n    text = re.sub(r\"\\S+@\\S+\", \"\", text)\n    text = re.sub(r\"\\d+\", \"\", text)\n    text = text.translate(str.maketrans(\"\", \"\", string.punctuation))\n    text = re.sub(r\"\\s+\", \" \", text).strip()\n    tokens = text.split()\n    if NLTK_AVAILABLE and lemmatizer:\n        tokens = [lemmatizer.lemmatize(w) for w in tokens if w not in stop_words and len(w) > 2]\n    else:\n        basic = {\n            \"the\", \"a\", \"an\", \"is\", \"are\", \"was\", \"were\", \"be\", \"been\", \"being\",\n            \"have\", \"has\", \"had\", \"do\", \"does\", \"did\", \"will\", \"would\", \"could\",\n            \"should\", \"may\", \"might\", \"must\", \"shall\", \"can\", \"need\", \"to\", \"of\",\n            \"in\", \"for\", \"on\", \"with\", \"at\", \"by\", \"from\", \"as\", \"into\", \"through\",\n            \"during\", \"before\", \"after\", \"above\", \"below\", \"between\", \"under\",\n            \"and\", \"but\", \"or\", \"yet\", \"so\", \"if\", \"because\", \"although\", \"though\",\n            \"while\", \"where\", \"when\", \"that\", \"which\", \"who\", \"whom\", \"whose\",\n            \"what\", \"this\", \"these\", \"those\", \"i\", \"me\", \"my\", \"myself\", \"we\",\n            \"our\", \"you\", \"your\", \"he\", \"him\", \"his\", \"she\", \"her\", \"it\", \"its\",\n            \"they\", \"them\", \"their\", \"am\", \"s\", \"t\"\n        }\n        tokens = [w for w in tokens if w not in basic and len(w) > 2]\n    return \" \".join(tokens)\n\ndef extract_features(text):\n    text_lower = text.lower()\n    urgency = [\n        \"urgent\", \"asap\", \"immediately\", \"critical\", \"emergency\",\n        \"broken\", \"down\", \"not working\", \"failed\", \"error\", \"bug\"\n    ]\n    return {\n        \"urgency_score\": sum(1 for w in urgency if w in text_lower),\n        \"word_count\": len(text.split()),\n        \"char_count\": len(text),\n        \"question_count\": text.count(\"?\"),\n        \"exclamation_count\": text.count(\"!\")\n    }\n\ndef predict_priority(text, label):\n    text_lower = text.lower()\n    urgency_words = [\"urgent\", \"asap\", \"immediately\", \"critical\", \"emergency\",\n                     \"down\", \"broken\", \"not working\", \"failed\", \"outage\",\n                     \"server down\", \"system down\", \"production\", \"crash\"]\n    high_count = sum(1 for w in urgency_words if w in text_lower)\n    if label in [\"Incident\", \"Problem\"]:\n        if high_count >= 2:\n            return \"high\"\n        elif high_count >= 1:\n            return \"medium\"\n        return \"medium\"\n    elif label == \"Change\":\n        return \"medium\"\n    else:\n        if high_count >= 1:\n            return \"medium\"\n        return \"low\"\n'); print('done')"